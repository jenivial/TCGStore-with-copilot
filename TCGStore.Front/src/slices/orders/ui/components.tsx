import React from 'react';

const OrderList = () => {
    // Placeholder for order list component
    return (
        <div>
            <h2>Order List</h2>
            {/* Render list of orders here */}
        </div>
    );
};

const OrderDetail = ({ order }) => {
    // Placeholder for order detail component
    return (
        <div>
            <h2>Order Detail</h2>
            {/* Render order details here */}
        </div>
    );
};

export { OrderList, OrderDetail };